namespace IMP4CMACGM.Core.Common
{
    public class ZipFileGeneration
    {

        public ZipFileGeneration(string codeFolder, string zipFolderPath)
        {

            //Ionic.Zip.ZipFile zip = new Ionic.Zip.ZipFile(Path.Combine(codeFolder, zipFolderPath));
            //if (Directory.Exists(Path.Combine(zipFolderPath, "img")))
            //    zip.AddDirectory(Path.Combine(zipFolderPath, "img"), "\\img");
            //if (Directory.Exists(Path.Combine(zipFolderPath, "data")))
            //    zip.AddDirectory(Path.Combine(zipFolderPath, "data"), "\\data");

            //if (Directory.Exists(Path.Combine(zipFolderPath, "audio")))
            //    zip.AddDirectory(Path.Combine(ZipoutputFolder, "audio"), "\\audio");

            //zip.Save();

        }
    }
}
